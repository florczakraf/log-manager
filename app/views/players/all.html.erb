<h1>Players</h1>
<hr />

<%= form_tag("/players/all", :metdod=>"get") do %>
		<%= text_field_tag "guid", nil, placeholder: "GUID" %>
		<%= submit_tag("Search", :class=>"btn") %>
	<% end %>
<hr />

<div id="container">
<%= will_paginate @players, :params => { :guid => @guid } %>
<table class="text-center table">
<tr class="well text-uppercase" style="font-family: monospace">
	<td>ID</td>
	<td>GUID</td>
	
	<td>Aliases</td>
</tr>

<% @players.each do |player| %>
	<tr>
		<td><%= player.id %></td>
		<td><%= player.guid %> <%= link_to image_tag("ggc.jpg"), "http://www.ggc-stream.net/search/server/guid/gid/#{player.guid}", :target => "_blank" %></td>
		
		<td>comming soon</td>
	</tr>
<% end %>

</table>
<%= will_paginate @players, :params => { :guid => @guid } %>
</div>

